# Daily Summary — March 23, 2026

## What We Accomplished

### 1. API Documentation (All Endpoints)

Wrote per-endpoint `.md` specs for every remaining service, following our standard format with request/response shapes, status codes, and error cases.

- **Dining Halls** — 1 endpoint doc (`get-menus.md`)
- **Scan** — 2 endpoint docs + README
- **Community** — 9 endpoint docs + README (posts CRUD, likes, comments)
- **Profile** — 9 endpoint docs + README (profile CRUD, avatar, food log, password change)
- Updated `docs/api/README.md` to link all services

Total: **24 new documentation files** covering every API endpoint in the app.

### 2. Favorites API (Fully Implemented)

Built the food favorites feature end-to-end:

| Endpoint | What it does |
|----------|-------------|
| `POST /favorites` | Save a meal combo to favorites (JSONB snapshot, 409 on duplicate) |
| `DELETE /favorites/{id}` | Remove a favorite (ownership check, 403/404) |

Files created:
- `app/models/favorite.py` — SQLAlchemy model
- `app/schemas/favorite.py` — Pydantic request/response schemas
- `app/services/favorite_service.py` — Business logic (duplicate check, ownership validation)
- `app/routers/favorite.py` — FastAPI router with JWT auth

### 3. Database Initialization (Supabase)

Created `sql/init.sql` with all **16 tables** across 4 phases:

| Phase | Tables | Count |
|-------|--------|-------|
| 0 — Auth | `users`, `verification_codes`, `refresh_tokens` | 3 |
| 1 — Menu & Food | `restaurants`, `meal_types`, `foods`, `food_icons`, `menu_snapshots`, `menu_sections`, `food_nutrition`, `food_icon_assignments`, `menu_section_items` | 9 |
| 2 — User Preferences | `user_preferences`, `meal_logs`, `meal_log_items` | 3 |
| 3 — Favorites | `favorites` | 1 |

All tables successfully created in the Supabase SQL Editor. Confirmed visible in the Table Editor.

### 4. Connected FastAPI to Supabase

- Set up `.env` with the Supabase pooler connection string
- Resolved DNS issue (campus network blocks direct DB hostname → switched to session pooler)
- Discovered project is in **us-west-2** region (not us-east-1)
- Final working URL: `aws-0-us-west-2.pooler.supabase.com:5432`
- Server starts and connects successfully

### 5. Data Ingestion — Gordon Avenue Market

Wrote `scripts/ingest_json.py` to load Nutrislice JSON exports into the database. Ingested `Gordon_Avenue_Market_03-15-2026.json` (Dinner, March 15):

| Table | Rows Inserted |
|-------|---------------|
| `restaurants` | 1 (Gordon Avenue Market) |
| `meal_types` | 1 (Dinner) |
| `menu_snapshots` | 1 |
| `menu_sections` | 8 (1849, Buona Cucina, Capital City Pizza, Que Rico, Fired Up, Buckingham Bakery, Gordon Delicious, Great Greens) |
| `foods` | 58 |
| `food_nutrition` | 58 |
| `food_icons` | 13 (allergens + dietary labels) |
| `food_icon_assignments` | 196 |
| `menu_section_items` | 58 |

The script is reusable for any future JSON exports from the scraper.

---

## Commits

- `e94769b` — implement favorites API + init all DB tables in Supabase
- `2fb446f` — database initialization

## Current State

- **Branch:** `api-docs` (pushed to origin)
- **Server:** Runs locally via `uvicorn app.main:app --reload`, connects to Supabase
- **DB:** All 16 tables live in Supabase with real Gordon Avenue menu data
- **Working endpoints:** Auth (signup/signin/etc.) + Favorites (save/delete)
- **Documented endpoints:** All services (Auth, Dining Halls, Questionnaire, Profile, Homescreen, Community, Scan)

## Next Steps

1. **Dining Halls API** — endpoints for listing halls, getting menus/stations (tables + data already exist)
2. **Questionnaire API** — submit/get/update user preferences
3. **Profile API** — user profile CRUD, food logging
4. **Homescreen API** — menu summary, daily goals, combo recommendations
5. Ingest more dining hall JSON files (other halls/dates/meal types)
